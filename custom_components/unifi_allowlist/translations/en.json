{
  "config": {
    "step": {
      "user": {
        "title": "Connect to UniFi",
        "description": "Create an API key in UniFi under Settings → Integrations, then paste it here.",
        "data": {
          "host": "Controller URL",
          "api_key": "API key",
          "verify_ssl": "Verify the SSL certificate"
        }
      },
      "site": {
        "title": "Choose the site",
        "description": "Pick which site to police, and where approval prompts should go. Sites are listed alphabetically.",
        "data": {
          "site": "Site",
          "notify_service": "Notification targets"
        }
      }
    },
    "error": {
      "cannot_connect": "Could not reach the controller. Check the URL and port.",
      "invalid_auth": "The controller rejected that API key."
    },
    "abort": {
      "already_configured": "That site is already set up."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Wifi Access settings",
        "data": {
          "notify_service": "Notification targets",
          "enforced_ssids": "Only police these SSIDs (leave empty for all)",
          "block_first": "Block on sight, then ask",
          "scan_interval": "Check every",
          "lookback": "Also catch devices seen within",
          "max_per_run": "Stop and alert if more than this appear at once",
          "channel": "Android notification channel",
          "group": "Android notification group",
          "min_list_guard": "Refuse to enforce if the allow list is smaller than",
          "notify_gap": "Pause between notifications",
          "adopt_blocks": "Adopt blocks made in UniFi",
          "deny_names": "Always block these device names",
          "deny_unnamed": "Always block devices that report no name",
          "forget_in_unifi": "Also remove the client from UniFi when forgetting"
        },
        "data_description": {
          "adopt_blocks": "Each poll, move devices blocked in the UniFi UI into Denied, and re-apply blocks that were removed there. Off by default.",
          "deny_names": "One entry per name, case insensitive, * and ? wildcards allowed. Matching devices are blocked silently with no notification and no queue entry. Names are reported by the device and can be changed, so treat this as noise control rather than security.",
          "deny_unnamed": "Devices with no hostname at all. This catches a lot of legitimate hardware such as cameras and IoT gear, so check the waiting list before turning it on.",
          "forget_in_unifi": "Deletes the client record on the controller, the same as Forget in the UniFi UI - alias, fixed IP and history included, with no undo. Clients that have a name or a fixed IP set in UniFi are only unblocked instead, so nothing you configured there is destroyed."
        }
      }
    }
  }
}
